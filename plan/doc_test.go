package plan_test

import "testing"

// TestPackageBootstrap is a temporary smoke test that proves the initial
// package is discoverable by the Go toolchain. Session 1 replaces it with
// behavior-focused planning model tests.
func TestPackageBootstrap(t *testing.T) {
	t.Parallel()
}
